export interface IEventError {
  name: string | null,
  description: string | null,
  place: string | null,
  address: string | null,
  date: string | null,
  time: string | null,
  duration: string | null
}
